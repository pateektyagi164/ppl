%{
#include "y.tab.h"
#include<stdio.h>
#include<stdlib.h>
#include<stdbool.h>
char buffer[10000];
int idx=0;
%}
%%
a*b* {while(idx<yyleng){
       buffer[idx]=yytext[idx];
       idx++;
       };buffer[idx]='\0';return valid;}
\n  {idx=0;return NEWLINE;}
.    {printf("invalid character %s appeared\n",yytext);return invalid;yyterminate();}
%%
int yywrap(){
return 1;
}