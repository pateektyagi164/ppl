%{
#include <stdio.h>
#include <stdlib.h>

int yylex();
void yyerror(const char *s);
int count = 0;
%}

%token WORD

%%
program:
      /* empty */
    | program WORD { count++; }
;

%%

void yyerror(const char *s){
    printf("Error: %s\n", s);
}

int main(){
    printf("Enter text:\n");

    yyparse();

    printf("Total words = %d\n", count);

    return 0;
}