%{
#include<stdio.h>
#include<stdbool.h>
char word[1000];
bool palindrome=true;
int len=0;
%}
%x string
%%
\" {palindrome=true;len=0;BEGIN(string);}
<string>{
[^\"\n] {word[len++]=yytext[0];}
\n {BEGIN(INITIAL);}
[\"] {word[len]='\0';
     for(int i=0;i<len/2;i++){
        if(word[i]!=word[len-1-i]){
              palindrome=false;
              break;
         };
           i++; 
       
      };
     if(palindrome){
        printf("String \"%s \"is a palindrome \n",word);
         }
     else{
      printf("String \"%s \"is not a palindrome \n",word);
       };
BEGIN(INITIAL);
  
     
}
}
.|\n {}
%% 
int yywrap(){
return 1;

}
int main(){
yylex();
return 0;
}

