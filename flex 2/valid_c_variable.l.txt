%{
#include<stdio.h>
%}
%x multilinecomment
%x comment

%%
"/*" {BEGIN(multilinecomment);}
"//" {BEGIN(comment);}
<comment>{
\n {printf("\n");BEGIN(INITIAL);}
. {}
}
<multilinecomment>{
"*/" {BEGIN(INITIAL);}
.|\n {}

}


.|\n {printf("%s",yytext);}
%%
int yywrap(){
return 1;
}
int main(){
printf("\n");
yylex();
return 0;
}
