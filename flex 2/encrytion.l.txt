%{
#include<stdio.h>
%}

%%
[a-zA-Z] {if(yytext[0]>='a' && yytext[0]<='z'){
            int gap='z'-yytext[0];
            if(gap>=3){
               yytext[0]=yytext[0]+3;
               printf("%s",yytext);
            }
            else{
               yytext[0]='a'+(2-gap);
               printf("%s",yytext);

                }
                 
           }
           else{
            int gap='Z'-yytext[0];
            if(gap>=3){
               yytext[0]=yytext[0]+3;
               printf("%s",yytext);

            }
            else{
               yytext[0]='A'+(2-gap);
               printf("%s",yytext);

                }
           };
}
[^a-zA-Z]+ {printf("%s",yytext);}
%%
int yywrap(){
return 1;
}
int main(){
yylex();
return 0;
}