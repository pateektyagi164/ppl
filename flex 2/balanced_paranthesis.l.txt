%{
#include<stdio.h>
#include<stdbool.h>
int top=-1;
bool invalid=false;
%}

%%
"(" {top++;}
")" {if(top!=-1){
      top--;
     }
     else{
     invalid=true;
     return 0;
     }
}
. {}
%%
int yywrap(){
return 1;
}
int main(){
yylex();
if(!invalid && top==-1){
 printf("\n the given stream of paranthesis is balanced");
}
else{
printf("\n the given stream of paranthesis is not balanced");
};
return 0;
}
